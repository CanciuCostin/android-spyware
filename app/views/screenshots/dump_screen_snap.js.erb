var commandOutput = <%=raw commandOutput%>;
$( ".Terminal__body" ).trigger("write",[" msf > ", "", commandOutput ]);

if (commandOutput != "Operation Failed"){
    window.open("/files/dumps/screen_snaps/<%= fileName %>")
}

jQuery.ajax({
                    url: "/screenshots/redirect1",
                    type: "POST",
                    dataType: "script"
                });